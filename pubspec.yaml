name: stunning_ui
description: "A production-ready, highly interactive, and glassmorphic UI toolkit for Flutter."
version: 1.8.0
homepage: https://github.com/Siddhesh-crypt/stunning_ui
repository: https://github.com/Siddhesh-crypt/stunning_ui
issue_tracker: https://github.com/Siddhesh-crypt/stunning_ui/issues

topics:
  - ui
  - widgets
  - design-system
  - glassmorphism
  - theme

# CLI: `dart run stunning_ui:doctor` / `:init` (or globally after
# `dart pub global activate stunning_ui`).
executables:
  stunning_ui_doctor: doctor
  stunning_ui_init: init

environment:
  sdk: ^3.7.0
  # 3.41 required for ImageFilter.shader glass refraction + BackdropGroup blur
  # coalescing used by GlassSurface.
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # Glass refraction shader shipped with the package (used by GlassSurface).
  shaders:
    - shaders/glass_refraction.frag

  # To add assets to your package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
